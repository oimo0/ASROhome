const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];const loader=$('#loader'),header=$('#header'),progress=$('.progress');addEventListener('load',()=>setTimeout(()=>loader.classList.add('done'),650));setTimeout(()=>loader.classList.add('done'),2200);
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(el=>io.observe(el));requestAnimationFrame(()=>$$('.hero .reveal').forEach((el,i)=>setTimeout(()=>el.classList.add('visible'),850+i*160)));
addEventListener('scroll',()=>{header.classList.toggle('scrolled',scrollY>25);const h=document.documentElement;progress.style.width=(scrollY/(h.scrollHeight-innerHeight)*100)+'%'},{passive:true});
$$('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=$(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'});$('.mobile-nav').classList.remove('open');$('.menu').classList.remove('open')}}));
const menu=$('.menu');menu.addEventListener('click',()=>{menu.classList.toggle('open');$('.mobile-nav').classList.toggle('open')});
const cur=$('.cursor'),dot=$('.cursor-dot');addEventListener('pointermove',e=>{cur.style.left=e.clientX+'px';cur.style.top=e.clientY+'px';dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px';if(innerWidth>900){const x=(e.clientX/innerWidth-.5)*14,y=(e.clientY/innerHeight-.5)*14;$('.symbol-wrap').style.transform=`translate(${x}px,${y}px)`}},{passive:true});$$('a,button,.group-card').forEach(el=>{el.addEventListener('pointerenter',()=>cur.classList.add('active'));el.addEventListener('pointerleave',()=>cur.classList.remove('active'))});
$$('.magnetic').forEach(el=>el.addEventListener('pointermove',e=>{if(innerWidth<900)return;const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.12}px)`;}));$$('.magnetic').forEach(el=>el.addEventListener('pointerleave',()=>el.style.transform=''));
function stars(canvas,count,speed){const c=canvas.getContext('2d');let pts=[];function reset(){canvas.width=canvas.clientWidth*devicePixelRatio;canvas.height=canvas.clientHeight*devicePixelRatio;pts=Array.from({length:count},()=>({x:Math.random()*canvas.width,y:Math.random()*canvas.height,r:Math.random()*1.3*devicePixelRatio,a:.15+Math.random()*.55,v:(.05+Math.random()*.15)*devicePixelRatio}))}function draw(){c.clearRect(0,0,canvas.width,canvas.height);for(const p of pts){p.y+=p.v*speed;if(p.y>canvas.height)p.y=0;c.beginPath();c.arc(p.x,p.y,p.r,0,Math.PI*2);c.fillStyle=`rgba(120,190,255,${p.a})`;c.fill()}requestAnimationFrame(draw)}reset();draw();addEventListener('resize',reset)}stars($('#stars'),90,1);stars($('#particles'),55,.35);